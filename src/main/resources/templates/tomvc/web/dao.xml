<?xml version="1.0" encoding="utf-8"?>
<template>
	<name>dao</name>
	<filePath>Work/qitoon/app/recruit/implement/src/main/java/${packageName}/${moduleName}/dao/${subModuleName}</filePath>
	<fileName>${ClassName}Mapper.java</fileName>
	<content><![CDATA[
/**
 * 
 */
package ${packageName}.${moduleName}.dao<#if subModuleName != "">.${subModuleName}</#if>;


import ${packageName}.${moduleName}.base.BaseDao;
import ${packageName}.${moduleName}.bean.${ClassName};


/**
 * ${functionName}DAO接口
 * @ClassName: ${ClassName}ServiceImpl
 * @author ${functionAuthor}
 * @date ${functionVersion}
 * Copyright: Copyright (c) 2016 
 * Company:ToB中心
 */
public interface ${ClassName}Mapper{

	<#list interfaces as m>
		<#if m.selectType =="A">
		public int m.{name}(${ClassName} ${className});
		</#if>
		<#elseif m.selectType =="U">
		public int m.{name}(${ClassName} ${className});
		</#elseif>
		<#if m.selectType =="D">
		 public int m.{name}{ClassName}(String id);
		</#if>
		<#elseif  m.selectType == "S">
		 public {ClassName} m.{name}(m.{putParam});
		 </#elseif>
		 <#elseif m.selectType == "L">
		 public List<{ClassName}> m.{name}(m.{putParam});
		 </#elseif>
		</#elseif>
		<#else>
		 public m.{outputParam} m.{name}${ClassName}(m.{putParam});
	    </#else>
	    </#if>
	</#list>
}]]>
	</content>
</template>